new point {            
    x=10  y=20  z=30       
    print()                # Call the print() method
}
                      
class point            

    x y z

    ? "Hello, World!"                  

    func print             
        ? self 

    func braceStart
        ? "Welcome"

    func braceEnd
        ? "Goodbye!"
