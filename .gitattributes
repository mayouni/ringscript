# Auto detect text files and perform LF normalization
* text=auto

# Launchers: line endings decide whether they run at all
*.sh  text eol=lf
*.bat text eol=crlf

# Binary assets
*.png  binary
*.ico  binary
*.wasm binary
